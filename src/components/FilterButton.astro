---
import Link from './Link.astro'

interface Props {
  href: string
  filterName: string
  class?: string | null | undefined
}

const { href, filterName, class: className, ...props } = Astro.props
---

<Link type='local' href={href} {...props}>
  {filterName}
</Link>
