<h2 class:list={['heading heading-2']}>
  <slot />
</h2>

<style>
  .heading-2 {
    --heading-size: clamp(var(--text-lg), 1.1071rem + 0.7143vw, var(--text-2xl));
    font-size: var(--heading-size);
    font-weight: var(--font-weight-medium);
    line-height: 1.4;
    margin-bottom: 0.5em;
    color: var(--color-gray-50);
  }
</style>
